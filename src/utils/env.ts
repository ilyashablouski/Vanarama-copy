export enum Env {
  DEV = 'dev',
  UAT = 'uat',
  PRE_PROD = 'pre-prod',
  PROD = 'prod',
}

export default Env;

export const DEFAULT_REVALIDATE_INTERVAL = 43200;
export const DEFAULT_REVALIDATE_INTERVAL_ERROR = 60;
