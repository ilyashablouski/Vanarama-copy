const config = {
  errorMessage: 'Error: {{error}}',
  heroImage: {
    src: `${process.env.HOST_DOMAIN}/Assets/images/competitions/win-a-new-lease-of-life/competition-hero.png`,
  },
};

export default Object.freeze(config);
